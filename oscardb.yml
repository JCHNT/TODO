input {
  jdbc {
    # Utilisez le chemin trouvé avec la commande find
    jdbc_driver_library => "/opt/logstash/vendor/jdbc/postgresql-42.5.0.jar"
    jdbc_driver_class => "org.postgresql.Driver"
    jdbc_connection_string => "jdbc:postgresql://oscar-test.unilim.fr:5432/oscar"
    jdbc_user => "oscar"
    jdbc_password => "oscar"
    statement => "SELECT * FROM activity_discipline LIMIT 5"
    schedule => "* * * * *"
  }
}
output {
  elasticsearch {
    hosts => ["http://164.81.15.118:9200"]
    index => "test_debug"
  }
  # Ajout d'une sortie stdout pour voir les données
  stdout { codec => rubydebug }
}
